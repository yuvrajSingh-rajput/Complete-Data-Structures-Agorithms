#include<bits/stdc++.h>
using namespace std;

/*
1. Element can be push either from front or back.
2. In this type -> element can pop in one way == front.
3. FUNCTIONS: [push_back(), push_front(), pop_front()];
*/


int main(){
    
    return 0;
}