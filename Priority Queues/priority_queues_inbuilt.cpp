#include<bits/stdc++.h>
using namespace std;

// ? IT IS BY-DEFAULT MAX-PRIORITY QUEUE.

int main(){
    priority_queue<int> pq;

    /*

        1. isEmpty() ==> pq.empty();
        2. getSize() ==> pq.size();
        3. insert(element) ==> pq.push(element);
        4. getMax() ==> pq.top();
        5. removeMax() ==> pq.pop();

    */
   
    return 0;
}